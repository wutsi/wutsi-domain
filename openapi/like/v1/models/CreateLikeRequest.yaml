# CreateLikeRequest
type: object
properties:
  canonicalUrl:
    type: string
    format: uri
    nullable: false
  deviceUUID:
    type: string
    format: uuid
    nullable: true
  userId:
    type: integer
    format: int64
    nullable: true
required:
  - canonicalUrl
